# SMTP configuration for feedback emails
# Example for Gmail (requires App Password; 2FA must be enabled):
# - Set SMTP_HOST to 'gmail' or 'smtp.gmail.com'
# - Use port 465 (secure)
# - SMTP_USER is your full Gmail address
# - SMTP_PASS is a Gmail App Password (NOT your login password)
SMTP_HOST=gmail
SMTP_PORT=465
SMTP_USER=youraccount@gmail.com
SMTP_PASS=your_gmail_app_password

# From address shown in outgoing emails
SMTP_FROM="Aryan Enterprises <youraccount@gmail.com>"

# Destination for feedback emails
FEEDBACK_TO=feedback@yourdomain.com
